import 'package:flutter/material.dart';

/// first_screen.dart
class WidgetKotak extends StatelessWidget {
  const WidgetKotak({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.white,
        width: 100,
        height: 100,
      ),
    );
  }
}
